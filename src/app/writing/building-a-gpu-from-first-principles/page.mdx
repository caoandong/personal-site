import { GpuHighLevelVisualization } from './gpu_visualization'

# Building a GPU from First Principles

what is a GPU?

at the most basic level, the GPU is a machine that executes instructions on data.
so the input is instruction + data, and the output is the processed data.

<GpuHighLevelVisualization />

for example, in the figure above, the GPU executes a vector add operation on array `A` and `B` and outputs `C`.


