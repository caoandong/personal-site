[project]
name = "continual-learning-01"
version = "0.1.0"
description = "Split CIFAR-10 benchmark for class-incremental learning"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.1.0",
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "torch>=2.10.0",
    "torchvision>=0.25.0",
]
